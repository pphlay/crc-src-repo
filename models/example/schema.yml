
version: 2

models:
    - name: dim_channelxxxx
      description: "A starter dbt model"
      columns:
          - name: sales_channel_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: dim_product
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
